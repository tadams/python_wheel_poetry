bundle:
  name: python_wheel_poetry

include:
  - ./resources/job.yml

workspace:
  host: https://dbc-656bfce3-2e54.cloud.databricks.com/

artifacts:
  default:
    type: whl
    build: poetry build

targets:
  dev:
    default: true
    mode: development
